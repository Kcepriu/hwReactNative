import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  btnLogOut: {
    borderWidth: 0,
    marginRight: 16,
  },
  btnToPublications: { borderWidth: 0, marginLeft: 16 },
});
